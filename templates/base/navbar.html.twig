{% from 'macros.html.twig' import PlayerBadge %}

<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom">
	<div class="container">
		<a class="navbar-brand fw-bold" href="{{url_for('home')}}">{{app.name}}</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				{% if user.has('ADMIN') %}
					<li class="nav-item">
						<a class="nav-link text-danger fw-bold" aria-current="page" href="{{url_for('tgdb')}}">
							<i class="fas fa-shield-halved"></i>
							TGDB</a>
					</li>
				{% endif %}
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{url_for('rounds')}}">
						<i class="fas fa-circle"></i>
						Rounds</a>
				</li>
				{% if user %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						<i class="fas fa-user"></i>
						My Data
					</a>
					<ul class="dropdown-menu">
						{# <li>
							<a class="dropdown-item" href="#"><i class="fas fa-circle"></i> My Rounds</a>
						</li> #}
						{# <li><hr class="dropdown-divider"></li> #}

						<li>
							<a class="dropdown-item" href="{{url_for('me.bans')}}"><i class="fas fa-hammer"></i> My Bans</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{url_for('me.notes')}}"><i class="fas fa-sticky-note"></i> My Notes</a>
						</li>
					</ul>
				</li>
				{% endif %}
			</ul>
			<ul class="navbar-nav">
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						{% if user %}
							{{PlayerBadge(user.getPlayerBadge, false)}}
						{% else %}
							Authenticate
						{% endif %}
					</a>
					<ul class="dropdown-menu">
						{% if user %}
							<li>
								<span class="dropdown-item disabled">Via
									{{user.getSource|capitalize}}</span>
							</li>
							<li><hr class="dropdown-divider"></li>
							<li>
								<a class="dropdown-item" href="{{url_for('logout')}}">Logout</a>
							</li>
						{% else %}
							<li>
								<a class="dropdown-item" href="{{url_for('auth.forum')}}">
									TGStation Forum</a>
							</li>
						{% endif %}
					</ul>
				</li>

			</li>
		</ul>

	</div>
</nav>
